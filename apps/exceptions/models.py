"""
TODO (together): ExceptionRecord — created when reconciliation can't
confidently match a transaction. Fields: transaction, reason, status
(open/investigating/resolved), resolution_note, resolved_by, ai_suggestion
(filled in later by the agent).
"""
